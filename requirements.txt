ultralytics>=8.0.0
opencv-python>=4.8.0
requests>=2.31.0
websockets>=12.0
flask>=3.0.0
flask-cors>=4.0.0
shapely>=2.0.0
# av (PyAV) decodifica câmeras RTSP preferindo hardware (NVDEC via hevc_cuvid/h264_cuvid)
# — bem mais leve de CPU que o VLC em software (ver ml_service/inference/pyav_camera.py).
av>=11.0.0
# python-vlc é só o binding — precisa ter o VLC instalado na máquina (libvlc.dll).
# Fallback pra câmeras RTSP que o PyAV/FFmpeg não consegue abrir de jeito nenhum
# (ver ml_service/inference/vlc_camera.py).
python-vlc>=3.0.0
# Controla a tomada Tuya que corta energia em risco grave (queda/invasão de
# zona) — ver orquestrador/hardware_alert.py.
tinytuya>=1.13.0
